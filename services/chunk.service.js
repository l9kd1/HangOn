var Chunk = require('../models/chunk.model');


/*
exports.getUserAdvancements = async function (query, page, limit) {

    try {
        var chunk = await Advancement.find(query).populate("chunks");
        return users;
    } catch (e) {
        throw Error('Erreur')
    }
}*/
